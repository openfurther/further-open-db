The FMDR (Federated Metadata Repository)
contains two main categories of metadata.

1) Assets (Asset* Tables)
These tables and views are in the fmdr.dll file.

2) End Users and their roles and privileges
These tables and views are in the fmdr_app_user.dll file.

